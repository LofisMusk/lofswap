[workspace]
resolver = "3"
members = [
    "blockchain-core",
    "explorer-api",
    "wallet-cli",
    "node-cli",
]
# Built with the Solana toolchain, not with the node workspace.
exclude = ["contracts/solana"]
